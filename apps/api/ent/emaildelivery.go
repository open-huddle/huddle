// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/open-huddle/huddle/apps/api/ent/emaildelivery"
	"github.com/open-huddle/huddle/apps/api/ent/invitation"
)

// EmailDelivery is the model entity for the EmailDelivery schema.
type EmailDelivery struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Status holds the value of the "status" field.
	Status emaildelivery.Status `json:"status,omitempty"`
	// FailureReason holds the value of the "failure_reason" field.
	FailureReason string `json:"failure_reason,omitempty"`
	// Recipient holds the value of the "recipient" field.
	Recipient string `json:"recipient,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EmailDeliveryQuery when eager-loading is set.
	Edges                 EmailDeliveryEdges `json:"edges"`
	invitation_deliveries *uuid.UUID
	selectValues          sql.SelectValues
}

// EmailDeliveryEdges holds the relations/edges for other nodes in the graph.
type EmailDeliveryEdges struct {
	// Invitation holds the value of the invitation edge.
	Invitation *Invitation `json:"invitation,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// InvitationOrErr returns the Invitation value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EmailDeliveryEdges) InvitationOrErr() (*Invitation, error) {
	if e.Invitation != nil {
		return e.Invitation, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: invitation.Label}
	}
	return nil, &NotLoadedError{edge: "invitation"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EmailDelivery) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case emaildelivery.FieldStatus, emaildelivery.FieldFailureReason, emaildelivery.FieldRecipient:
			values[i] = new(sql.NullString)
		case emaildelivery.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case emaildelivery.FieldID:
			values[i] = new(uuid.UUID)
		case emaildelivery.ForeignKeys[0]: // invitation_deliveries
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EmailDelivery fields.
func (_m *EmailDelivery) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case emaildelivery.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case emaildelivery.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case emaildelivery.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = emaildelivery.Status(value.String)
			}
		case emaildelivery.FieldFailureReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field failure_reason", values[i])
			} else if value.Valid {
				_m.FailureReason = value.String
			}
		case emaildelivery.FieldRecipient:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field recipient", values[i])
			} else if value.Valid {
				_m.Recipient = value.String
			}
		case emaildelivery.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field invitation_deliveries", values[i])
			} else if value.Valid {
				_m.invitation_deliveries = new(uuid.UUID)
				*_m.invitation_deliveries = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EmailDelivery.
// This includes values selected through modifiers, order, etc.
func (_m *EmailDelivery) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryInvitation queries the "invitation" edge of the EmailDelivery entity.
func (_m *EmailDelivery) QueryInvitation() *InvitationQuery {
	return NewEmailDeliveryClient(_m.config).QueryInvitation(_m)
}

// Update returns a builder for updating this EmailDelivery.
// Note that you need to call EmailDelivery.Unwrap() before calling this method if this EmailDelivery
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *EmailDelivery) Update() *EmailDeliveryUpdateOne {
	return NewEmailDeliveryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the EmailDelivery entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *EmailDelivery) Unwrap() *EmailDelivery {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: EmailDelivery is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *EmailDelivery) String() string {
	var builder strings.Builder
	builder.WriteString("EmailDelivery(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("failure_reason=")
	builder.WriteString(_m.FailureReason)
	builder.WriteString(", ")
	builder.WriteString("recipient=")
	builder.WriteString(_m.Recipient)
	builder.WriteByte(')')
	return builder.String()
}

// EmailDeliveries is a parsable slice of EmailDelivery.
type EmailDeliveries []*EmailDelivery
